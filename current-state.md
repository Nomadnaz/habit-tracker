# current-state.md — habit-tracker
> Living snapshot. Update this file every time a task in `tasks/` is verified done, then commit and push.
> Read this AFTER system-model.md and database.md, BEFORE picking up the next task file.

Last updated: 2026-08-04 (**Apple Calendar/Reminders catch-up backfill fixed** — device-created tasks now get their Apple entry on next app open even if the app was fully shut when the task was created; see Progress log. Not yet on TestFlight.)

Previously: 2026-07-24 (**first real on-device verification pass started** — TestFlight shipping pipeline stood up end-to-end (Apple Developer account, EAS credentials, App Store Connect app record + API key), and the first real device test immediately surfaced and fixed 3 genuine bugs no prior tsc/vitest-only session had ever caught — see Progress log. Apple Health steps sync now confirmed working on a real iPhone. Stage 1 (the on-device verification gate) is now actively underway — see NEXT TASK.)

Previously: 2026-07-10 (Code Audit v2 fix plan, P0+P1+P2 done — see `SecondBrain/Projects/Habit Tracker/Build/2026-07-09 Code Audit v2 — Fix Plan.md` for the full prioritized plan; P2's code is committed and migration 029 is live, but **`ai-chat` itself still needs a manual redeploy** — auto mode's safety classifier blocked an inferred production deploy, see Action Needed below)

---

## WHAT'S ACTUALLY BUILT TODAY (before this task breakdown started)

| Area | File(s) | State |
|---|---|---|
| Auth | `app/_layout.tsx`, `app/(auth)/login.tsx` | Working — Supabase session guard, redirects to login/tabs |
| Today screen | `app/(tabs)/index.tsx` (2270 lines), `components/BriefingCard.tsx`, `supabase/functions/daily-briefing/` | Working — tasks, focus timer, scroll wheel UI. **Daily briefing added (tasks 018+019), not yet on-device verified**: a card below the header shows a cached AsyncStorage briefing instantly, with a manual refresh calling the new `daily-briefing` Edge Function (reads only the user's `briefing_preferences.selected_modules` via `buildContext`, <150-word Haiku summary). Needs `supabase functions deploy daily-briefing`. |
| Gym / Body hub | `app/(tabs)/gym.tsx` (slimmed, 2026-07-07), `app/strength.tsx` (new), `app/workouts.tsx`, `app/workout-detail.tsx`, `app/steps.tsx`, `lib/workout-data.ts`, `lib/body-data.ts`, `lib/bodyFormulas.ts` (new) | Working — **de-faked + split, 2026-07-07** (see progress log's "Cal AI/Strava parity pass" entry): `seedData()`'s fabricated steps/training history, headline lifts, weakest-muscle, strength-trend, sleep, protein are gone; every field is real or an honest `—`/empty state. Full lift/muscle/strength-trend detail moved to new `app/strength.tsx`; the hub is now a compact preview linking there. Water/weight logging, WEEK PLAN, movement pills unchanged (already real). |
| Calendar | `app/calendar/index.tsx`, `app/calendar/day.tsx` | Working |
| Focus timer | `app/focus-timer.tsx` (1372 lines) | Working, standalone — not yet wired to app-blocking (that's FUTURE/entitlement-gated) |
| Profile / Progress (hidden tabs, reachable via icon) | `app/(tabs)/profile.tsx`, `progress.tsx` (rebuilt 2026-07-07) | **Nav-restructured 2026-07-10** (task 078): no longer tab-bar slots — both registered with `href: null`, reachable via `router.push` (Today header → profile; Fitness header chart icon → progress). Profile: badge grid + new sign-out button. Progress: Strava-style stats page (activity calendar, trophy case, all-time stats via `getCumulativeStats()`), unchanged content. `tree.tsx` **deleted** (dead — `bonsai` table dropped from prod 07-06). |
| Health / Life hubs | `app/(tabs)/health.tsx`, `life.tsx` (new 2026-07-10) | **Built (Code Audit v2 fix plan P1)** — pure card-grid hubs, no new logic, each card `router.push`es an existing screen. Health: calories, sleep, mood, steps, body-log (→ Fitness tab), cycle tracking. Life: goals, finance, library, focus timer, calendar. Not yet on-device verified. |
| Settings | `app/settings/index.tsx`, `app/settings/companion-persona.tsx`, `supabase/functions/save-api-key/`, migration `014_user_api_keys.sql` | **Built (task 020), not yet on-device verified** — API-key toggle (own key, AES-256-GCM encrypted server-side, vs app credits). `ai-chat`'s `getUserApiKey()` consumes a saved key (v23, live). **`save-api-key` deployed 2026-07-15** (version 1, first-ever deploy) — the toggle is now functional end-to-end, both storage and consumption sides live. Per-companion name/photo editor reachable via a new gear icon in the Today header, not a tab (see task notes). |
| AI companions | `supabase/functions/ai-chat/`, `_shared/companions.ts`, `_shared/buildContext.ts`, `lib/companions.ts`, `components/ChatScreen.tsx`, migration `006_ai_companions.sql` | **Deployed version is v23 (2026-07-15)** — P2's SharedContext work is live: buildContext.ts has a cross-domain SharedContext block (calories/protein/sleep/tomorrow's plan/steps/focus/water for every companion regardless of its own contextSources), `daily_steps` table (migration 029, applied live) + upsert wiring, `focus_sessions` writes from the app's own focus timer, and a working `remember_about_user` action. **P3 done**: 12 config-object companions now (`habitCoach`/`life`/`gym`/`focus`/`calorie`/`activity`/`sleep`/`goals`/`mood`/`medication`/`finance`/`library` — the last 3 are Vital/Ledger/Stacks, added with matching `buildContext.ts` blocks over `medications`+`medication_logs`, `expenses`+`bills`+`budgets`, `books`+`movies`+`saved_links`+`ideas`). Committed but **P3's code is not yet redeployed to `ai-chat`** (each deploy needs its own explicit human go-ahead — confirmed this session, a generic "yes continue" did not authorize the `save-api-key` deploy on its own). `life` has a real entry point (the LIFE tab hub); `focus` remains unwired to a chat UI. `006` is live in the DB; data verified clean (no date corruption). Key is a Supabase function secret only. Action execution + confidence gates: see tasks/012+039. |
| Voice device (Companion HUD) | `~/esp/projects/companion-hud` (firmware) + `tools/phone_sim.py` (bridge) | **Working end-to-end** — a NEW "surface" that bolts onto task 013's `ai-chat` contract `{ message, companionType, conversationHistory } → { response, actions }`. Voice → BLE → `phone_sim.py` → Whisper STT (local) → `ai-chat` → grounded answer back on the device ASK screen. No firmware change needed for AI. Not on the original 77-task roadmap (it's a surface, not a feature). |
| Habits | `app/(tabs)/habits.tsx`, `lib/habits-data.ts`, `components/HeatmapCalendar.tsx`, migrations `009_habits.sql`+`022_streak_freezes.sql` | **Built (tasks 022+023+076), not yet on-device verified** — new HABITS tab (list, per-habit streak, green/red/grey/blue heatmap, completion toggle, add-habit flow, per-habit auto-freeze toggle). Local-first via `lib/habits-data.ts`; per-habit streak computed from `habit_logs` (not the generic entity-keyed `lib/streaks.ts`). Streak freezes (task 076): 2/month auto-applied when enabled per habit, shown blue on the heatmap; repair/holiday intentionally not built (separate future sub-task). Needs both migrations run. |
| Medication | `lib/medications-data.ts`, `app/(tabs)/habits.tsx` (MEDS toggle), migration `010_medications.sql` | **Built (task 024), not yet on-device verified** — sub-section of the Habits screen (segmented toggle, not its own tab): per-medication heatmap/streak, 30-day adherence %, 'Day X of Y' course progress. Needs migration `010_medications.sql` run. |
| Calorie / Nutrition | `app/calorie/{index,capture,confirm,history}.tsx` (split 2026-07-07, was one file), `lib/meals-data.ts`, `lib/foodVision.ts`, `lib/nutritionFormulas.ts` (new), `supabase/functions/food-vision/`, migration `007_nutrition.sql` + new `025_meal_photo_storage.sql` | **De-faked + split, 2026-07-07**: `foodVision.ts`'s hardcoded 520-calorie mock fallback is gone (returns `null` on failure → blank editable confirm screen with photo attached, never fabricated numbers). Nutrition targets now a real Mifflin-St Jeor `computeTargets()` from onboarding's age/sex/height/weight, wired into `app/(onboarding)/targets.tsx`. Meal photos now upload to a real private Storage bucket (migration `025`, **applied 2026-07-08**). Screen split into dashboard/capture/confirm/history matching Cal AI's structure. **`food-vision` deployed 2026-07-09** (v1, `ANTHROPIC_API_KEY` secret already set on the project) — real AI estimates should now work; needs an on-device snap-a-meal check to confirm. |
| Activity (hike/run/walk) | `app/(tabs)/activity.tsx` (record-only, 2026-07-07), `app/activity-summary.tsx` (new, task 033), `app/activity-history.tsx` (new), `lib/activity-data.ts`, `lib/activityFormulas.ts` (new), `lib/locationTask.ts` (new), migration `012_activity.sql` | **Split + extended, 2026-07-07, not yet on-device verified**: the tab is now record-only; `stop()` routes to the new `activity-summary` screen (task 033, finally built — real per-km splits via `computeSplits()`, real elevation profile via `components/ElevationGraph.tsx`, honest empty state if no altitude was recorded). `activity-history.tsx` is a full browsable list beyond the tab's old inline "recent" section. **Background location is now implemented** (`lib/locationTask.ts`, `expo-task-manager` + `app.json` `UIBackgroundModes`/plugin config) but is **explicitly unverified** — no device, no EAS dev build, no battery-drain test; foreground-only tracking (previously verified) remains the automatic fallback if background permission is denied. Needs migration `012` run. |
| Sleep | `app/modals/sleep-detail.tsx`, `lib/sleep-data.ts`, migration `011_sleep.sql` | **Built (tasks 035+036), not yet on-device verified** — manual sleep log (bedtime/wake/quality) + weekly bar chart + the Phone Down Challenge (manual entry; iOS Sleep Focus auto-detection is device-gated, task 042). Reachable from the Body hub's SLEEP recovery card (`app/(tabs)/gym.tsx`). Needs migration `011_sleep.sql` run. |
| Onboarding | `app/(onboarding)/*.tsx` (10 screens), `components/OnboardingShell.tsx`, `lib/onboarding-data.ts`, `lib/featureFlags.ts`, migration `013_user_profiles.sql`, `app/_layout.tsx` (extended) | **Built (task 062), NOT yet on-device verified — highest-risk change this session, changes the auth-entry routing.** welcome→basics→goals→targets→first-habit→skills→book→connect→account→briefing-builder; account creation is screen 9. A local `@onboarding_complete` flag (independent of login) decides whether a logged-out user sees onboarding or login. See tasks/062 notes for a real ordering bug that was caught and fixed (signUp() making a session appear one screen early) and for what a currently logged-out tester will see on first launch after this update. |
| Badges | `lib/badges.ts`, `app/(tabs)/profile.tsx` (badge grid), migrations `015_badges.sql`+`016_cumulative_stats.sql` | **Built (task 063), not yet on-device verified** — launch set of 10 badges, checked from `postWrite`'s badge step (previously a no-op stub). Push notification on unlock is NOT implemented (no notification infra). Also fixes a long-standing gap: `cumulative_stats` (postWrite step 1) had never been created — real now. |
| Goals | `app/modals/goals.tsx`, `lib/goals-data.ts`, migration `017_goals.sql`, new `goals` companion | **Built (task 068), not yet on-device verified** — structured goals/milestones/progress (vision board intentionally not built, optional polish). Reachable via a new flag icon on the Today header. New `goals` companion wired into `buildContext`. |
| Finance | `app/modals/finance.tsx`, `lib/finance-data.ts`, migration `018_finance.sql` | **Built (task 065), not yet on-device verified** — Spending/Bills/Budgets as a segmented toggle (not a tab — see task notes), manual entry only, over-budget flag per category. Reachable via a new cash icon on the Today header (now 7 icons — getting crowded, flagged not fixed). |
| Mood | `app/modals/mood.tsx`, `lib/mood-data.ts`, migration `019_mental_health.sql` | **Mood log built (task 066), not yet on-device verified; journal/therapy deliberately NOT built** — no crypto dependency installed, no device to verify one, and the task's own gate ("before a single field ships") makes shipping zero fields the honest call over guessing at encryption. See task notes. |
| Cycle tracking | `app/modals/cycle-tracking.tsx`, `lib/cycle-data.ts`, migration `020_cycle.sql` | **Mostly built (task 067), Face ID gate deliberately NOT built** — opt-in only (Settings row, off by default), never referenced by buildContext at all, never enters postWrite's fan-out. No `expo-local-authentication` installed, no device to verify a biometric flow — see task notes (same reasoning as journal/therapy encryption). |
| Library | `app/modals/library.tsx`, `lib/library-data.ts`, migration `021_library.sql` | **Built (task 064), manual entry only, not yet on-device verified** — Books/Movies/Links/Ideas segmented toggle (not a tab), a heuristic (not API-backed) natural-language capture classifier. No Google Books/TMDB integration — no API keys. Reachable via a new book icon on the Today header (now 9 icons — needs a real redesign). |
| Global Search | `components/GlobalSearch.tsx`, `app/modals/search.tsx` | **Built (task 074), not yet on-device verified** — searches habits/goals/library/finance/workouts/tasks (local AsyncStorage, offline-capable), never touches journal/therapy/mood (structurally — those stores are just never read). Only goals/tasks deep-link to the specific record; others navigate to the section. Reachable via a new magnifying-glass icon next to "TODAY". |
| Obsidian sync (schema only) | migration `023_vault_files.sql` | **Table + FTS index built (task 057), nothing writes to it yet** — the actual sync client needs iCloud file access, device-gated, not built this session. |
| Everything else in the spec (focus-blocking, social, wearables, integrations, monetisation) | none | **Not started**, correctly FUTURE per the spec |

## SCHEMA REALITY CHECK
The existing app uses its own table names that **do not match** the spec's proposed schema:
```
workout_templates, workout_exercises, workout_done_log, pb_log, body_weight_logs, water_logs
```
vs. the spec's proposed:
```
workouts, personal_bests, body_logs
```
`exercises` is the one table that already matches. No `supabase/migrations/` folder exists — schema changes have been applied via loose `.sql` files run manually in the Supabase SQL editor (`run-this-once.sql`, `workout-schema.sql`, `user_focus.sql`, `user_focus_durations.sql`, `task_schedule_columns.sql`). See `database.md` for the reconciliation plan and `tasks/002` / `tasks/005` for the migration work.

## KNOWN CONFLICTS WITH THE OLD CLAUDE.md (now corrected)
- Date keys were `"YYYY-M-D"` (0-indexed month, e.g. `"2026-5-1"` for June 1). Canonical is now zero-padded `YYYY-MM-DD`, 1-indexed. **Not yet migrated in code** — see `tasks/003`/`tasks/004`.
- Design tokens were light theme (`#F5F5F5` bg, `#E0E0E0` border). Canonical is dark (`#0A0A0A` bg, `#FF4D00` accent, `#2A2A2A` border, PressStart2P/SpaceMono). **Not yet migrated in code** — see `tasks/077`, deliberately last (cosmetic, non-blocking).
- ~~Old 4-tab layout~~ — **done 2026-07-10** (Code Audit v2 fix plan P1, task 078): 5 canonical tabs now registered (Today/Habits/Health/Fitness/Life), `tree.tsx` deleted, `activity`/`progress`/`profile` kept as `href: null` hidden tab routes (still reachable via `router.push`, no tab-bar slot). See progress log.

## GIT STATE (as of this architecture pass)
- Remote: `https://github.com/Nomadnaz/habit-tracker.git`, branch `main`
- Local was 2 commits ahead of origin, with uncommitted changes to `app.json`/`package.json`/`package-lock.json` before this session
- This session adds: `system-model.md`, `database.md`, `current-state.md` (this file), rewritten `CLAUDE.md`, `tasks/001`–`tasks/077`

## NEXT TASK
**P0–P5 (both phases) are done in code** (P2 code deployed 2026-07-15 as `ai-chat` v23; P3's companion-roster code is committed but rides on its own redeploy of `ai-chat`, not yet requested; P4 committed `0d25fa4`, not yet on-device verified; P5 phases 1+2 committed 2026-07-16, see below — **P5's original framing was revised this session**: `tools/vault-agent` turned out to be a single-hardcoded-account dev tool that can never serve real users, not a rollout step, so it's staying dev-only rather than being launched). Nothing from P5 has been run on a real device yet — that's the next real gate. See `handover-5-second-brain-multi-user-and-export.md` for the full session writeup and `/Users/nova/.claude/plans/how-am-i-going-memoized-ember.md` for the design.

**Working plan: `SecondBrain/Projects/Habit Tracker/Build/2026-07-09 Code Audit v2 — Fix Plan.md`** (analysis: same folder, `— Analysis.md`). Phases:
- ~~**P0 — hygiene**~~ ✅ done 2026-07-10.
- ~~**P1 — nav restructure**~~ ✅ done 2026-07-10 (task 078 resolved: enforce the 5-tab plan).
- ~~**P2 — SharedContext**~~ ✅ done 2026-07-15 — code done 2026-07-10, migration 029 applied live, `ai-chat` redeployed to v23 2026-07-15.
- ~~**P3 — companion roster**~~ ✅ code done 2026-07-15 (`medication`/`finance`/`library` configs + buildContext blocks added, committed `d549057`) — **needs its own `ai-chat` redeploy** (currently live v23 predates this commit; not yet requested).
- ~~**P4 — postWrite integrity**~~ ✅ done 2026-07-16 (commit `0d25fa4`): `lib/library-data.ts`'s `setBookStatus`/`markWatched` now call `postWrite('book'|'movie', …, 'update')` on finish/watch (Entity union extended, `incrementCumulativeStats` relaxed to count these two update events so `total_books_finished`/`total_movies_watched` can finally increment); `deleteMeal()` now calls `postWrite('meal', …, 'delete')`; `lib/body-data.ts`'s `addWater`/`logWeight` now call `postWrite('water'|'weight', …, 'create')` (the Entity union already had these, just unused). `tsc`/`vitest` held at the pre-existing baseline (32/32 pass). Not verified on-device.
- ~~**P5 phase 1 — server-native vault notes**~~ ✅ done 2026-07-16: `lib/obsidian.ts`/`lib/obsidianNotes.ts` give `postWrite`'s `writeObsidian()` a real body — writes AI-readable `.md`-formatted notes directly to `vault_files` (Supabase), no filesystem/iCloud/Obsidian involved. Scope: task/meal (appended into the day's Daily Note, idempotent via an `<!--id:...-->` marker so repeat updates replace rather than duplicate a line), habit (rolling per-habit log, same marker pattern keyed by date), workout/goal/book/movie (one file each, regenerated on each status change). Deliberately skips sleep/mood/water/weight/medication/focus/activity/expense — thin deltas already fully captured in structured tables, a note adds nothing `buildContext.ts` doesn't already give the AI. Deletes soft-delete via the existing `deleted_at` column (meals only — removes just that line from the Daily Note, not the whole file). Also had to add `date`/`name`/`title` fields to several existing `postWrite()` call sites (`lib/actionExecutor.ts`'s 3 task cases, `lib/habits-data.ts`, `lib/goals-data.ts`, `lib/workout-data.ts`, `lib/library-data.ts`) that previously only passed bare ids — all cheap local lookups, no new network calls. **`tools/vault-agent` was found to be single-hardcoded-account (one email/password, one Mac folder path) and cannot serve real users — explicitly kept dev-only, not launched, current-state.md's old "P5" framing was wrong to treat it as the rollout step.** 32 new vitest cases (`lib/obsidian.test.ts`, pure-logic only — no Supabase mocking needed, same pattern as `lib/bodyFormulas.test.ts`). Not verified on-device.
- ~~**P5 phase 2 — "Export to Obsidian"**~~ ✅ done 2026-07-16: `lib/obsidianExport.ts` fetches the user's non-deleted `vault_files` rows, builds a real `.zip` (folder structure preserved: `Second Brain/Daily Notes/`, `/Habits/`, `/Workouts/`, `/Goals/`, `/Library/`) via `jszip`, writes it via `expo-file-system`'s SDK 54 `File`/`Paths` API (verified against the installed `.d.ts`, not assumed from memory), shares it via `expo-sharing`'s OS share sheet. New "SECOND BRAIN" section in Settings (`app/settings/index.tsx`) with the export button. New deps: `jszip`, `expo-sharing` (installed via `npx expo install`). One-shot snapshot export, not live sync — the deliberate tradeoff for zero cost/entitlement; a real live round-trip (iCloud + `lib/vaultSync.ts`, task 059) stays a deferred future phase 3. Not verified on-device — needs a real login with some logged data, tapping the export button, and confirming the `.zip` actually opens as a valid Obsidian vault.
- **P6 — EAS dev build + device pass**: the actual verification gate for everything above, plus filing slow-clock applications (FamilyControls, CASA, IAP).
- ~~**Tasks 060+061 — vault action gaps**~~ ✅ code done 2026-07-16 (picked up standalone while user was away from a device, not part of the P0-P6 audit plan): task 060's `search_vault` criterion was already satisfied by `buildContext.ts`'s existing FTS pre-fetch; its `remember_about_user` re-summarization gap and task 061's missing profile trend are now both closed in `daily-briefing/index.ts` + new `_shared/trends.ts`. Both ride on the same pending `daily-briefing` redeploy.

Domains beyond the spine (travel/social/wearables) remain correctly FUTURE per the spec.

## ⚠️ ACTION NEEDED BY YOU (cannot be done from this session)
### ⚠️ Standing rule
**NEVER run `supabase db push`** on this project — it re-runs the one-time-unsafe `002_date_key_format.sql` against live data. Use the Supabase MCP (connected) or paste individual migration files into the SQL editor.

### Migrations — all 29 live
Confirmed via `list_migrations` + `supabase/migrations/APPLIED.md` (2026-07-10, includes new `029_daily_steps.sql`, applied via Supabase MCP this session). Nothing pending.

### Edge Functions — 1 redeploy pending (P3), rest current
**`ai-chat` and `save-api-key` were both deployed 2026-07-15** (`ai-chat` → v23 with P2's SharedContext work live; `save-api-key` → v1, its first-ever deploy). `transcribe`, `daily-briefing`, `device-state`, `tts`, `food-vision` remain `ACTIVE`, no changes pending. **New gap: P3's companion-roster commit (`d549057`, 2026-07-15) postdates the `ai-chat` v23 deploy**, so `medication`/`finance`/`library` companions exist in code but aren't live yet — needs its own `supabase functions deploy ai-chat` once requested explicitly (each deploy needs its own human go-ahead per this project's rule; a generic "continue" does not count, confirmed twice this session).

### On-device verification — **underway as of 2026-07-24**, TestFlight pipeline now live
Real device testing has started via TestFlight (build 8 confirmed working) — see the 2026-07-22/23/24 progress log entries for the shipping-infra setup and the 3 real bugs the very first test already caught (react-dom/lock-file drift, a `.default` import bug that silently disabled HealthKit in every build ever made, and a New Architecture incompatibility in `react-native-health`). **Apple Health steps sync is now confirmed working on a real iPhone.** Everything below this line is the original pre-TestFlight checklist — work through it next, ticking items off as they're actually verified rather than assuming they're fine.

**Start with onboarding — it's the riskiest change (rewrites the login-entry routing) and the most important to check first.** The next time you open the app logged out, you'll see the new onboarding flow instead of the login screen directly (no way to tell "existing tester" from "new install" apart — see tasks/062 notes). Either run the full 10-screen flow once (confirm it ends by landing in the app with your first habit/targets/briefing prefs saved, then force-quit and reopen to confirm it does NOT re-onboard), or tap "I ALREADY HAVE AN ACCOUNT" on the welcome screen to skip straight to login.

Then, in whatever order is convenient:
- **Today/Calendar/Steps/Workouts** still look right after the date-key migration (tsc clean, never visually checked).
- **Habits tab**: add a habit, mark it done, confirm the heatmap turns green and the streak survives a restart; toggle auto-freeze on, skip a day, confirm it renders blue (task 076); switch to MEDICATION, add one with a course length, confirm adherence %/course progress.
- **Body (gym) tab**: tap the SLEEP recovery card, log a night + a Phone Down Challenge entry.
- **Activity tab**: grant location, take a short walk, confirm live stats + polyline, and confirm `stop()` now lands on the new activity-summary screen (splits + elevation profile) rather than staying on the tab. **New this session**: background location is implemented (`lib/locationTask.ts`) but needs an EAS dev build (not Expo Go) + a real 30-min+ outdoor walk to judge battery drain and whether tracking actually survives the app being backgrounded/killed — completely unverified, do this before trusting it.
- **Body/Strength/Progress**: confirm `app/(tabs)/gym.tsx`'s STRENGTH tile and TOP LIFT/STRENGTH cards link to the new `/strength` screen and show real (or honest `—`) data; confirm `app/(tabs)/progress.tsx` renders a real activity calendar/trophy case/stats table instead of the old dead stub.
- **Calorie**: confirm `/calorie` → SNAP A MEAL opens the new dedicated `/calorie/capture` screen (not an action-sheet alert), and that a failed/undeployed vision call now shows a blank editable confirm screen instead of a fake "Meal (estimate), 520 kcal" plate.
- ~~**Environment gap**: `npx expo start --web` could not be run~~ — **resolved 2026-07-22**: `react-dom`/`react-native-web` installed (was actually required, not optional — see progress log).
- **Today header icons** (search 🔍, calendar, calorie 🍎, bluetooth, goals 🚩, finance 💵, mood 🙂, library 📖, settings ⚙️, logout — **9 icons, genuinely overcrowded, flagged repeatedly this session, still not fixed**): goals (add + milestones), finance (expense/bill/budget + over-spend flag), mood (log + persists), library (paste a URL vs a plain title, confirm they land correctly), global search (search for anything, confirm navigation).
- **Settings**: toggle "use my own API key" on/off; set up a companion's name/photo; go to "Cycle tracking (off by default)", turn it on, log a period, confirm the prediction.
- **Daily briefing**: tap GET DAILY BRIEFING under the Today header, confirm a <150-word summary appears and persists.
- **Badges**: complete a habit/workout/run and check the PROFILE tab's badge grid + confirm `cumulative_stats`/`badges_earned` rows appear in the Supabase dashboard.

### Standing
- `git push` — done as of 2026-07-10 (P0 hygiene pass); keep pushing after every commit.

## HOW TO UPDATE THIS FILE
After a task is implemented, deployed, and tested:
1. Tick its acceptance criteria in the task file (or mark the file Status: DONE if fully verified).
2. Add a one-line entry below under "Progress log" with the date.
3. Update the "WHAT'S ACTUALLY BUILT TODAY" table above if the task changed a screen's state.
4. Commit and push.

## PROGRESS LOG
- 2026-08-04 — **Fixed: device-created tasks never reached Apple Calendar/Reminders unless the app happened to already be open.** User reported (talking about the voice device specifically): hold the ASK button while the phone/app is backgrounded or locked, the task lands in Supabase fine but never appears in Apple Calendar/Reminders — not delayed, permanently missing. Root cause was in `lib/use-remote-task-sync.ts`: `reconcile()` (the catch-up pull that runs on launch/foreground, meant to cover "anything created while the app was shut") called `applyRow(row, notify=false)`, and `applyRow` returned before its Apple-write block whenever `notify` was false — that gate existed only to suppress the "new task" banner on bulk catch-up, but it accidentally skipped the Apple write too. Only the live Realtime path (app already open when the row arrives) ever created the Reminder/Event. Fix: `reconcile()`'s per-row loop now does the same Apple-write dance the live path does — `findExistingAppleReminder()` first (race guard), else `syncNewTaskToApple()` — before inserting into the local map, guarded by the same `appleCreatedIds` set so it can't reintroduce the mass-duplication bug that made someone disable Apple-writes on this path in the first place. `tsc --noEmit` clean on both touched files (`lib/use-remote-task-sync.ts`, no changes to `lib/apple-sync.ts`); `vitest` 70/70 pass, unaffected (no pure-formula module touched). **Not on-device tested, not on TestFlight** — this is a code-only fix done remotely while the user was away from the hardware; still needs a real device test (create a task via the voice device with the app fully closed, then reopen the app and confirm the Reminder/Event appears) before it's verified, and a new EAS build + TestFlight submission before the user can test it at all.
  - This is also step 1 of a larger plan (Wi-Fi-direct voice pipeline for the ESP32 companion device, so voice logging works with the phone/app fully closed, not just backgrounded) — see `SecondBrain/Conversations/` for the full scoping; steps 2+ need the physical hardware and are blocked until the user is back with the device.
- 2026-07-22 to 2026-07-24 — **TestFlight shipping pipeline stood up end-to-end; first real on-device test immediately found and fixed 3 genuine bugs.** User obtained a paid Apple Developer account; this was the trigger to finally close the P6 on-device gate that every prior session's progress log entry had flagged as unreachable. Work:
  - **EAS/Apple credential chain**: `eas init` linked the repo to a new Expo project (`extra.eas.projectId` in `app.json`); iOS Distribution Certificate + Provisioning Profile generated (one-time interactive Apple ID + 2FA step, had to run in a real Terminal — this sandboxed environment's `!`-prefixed shell commands are not a true TTY and can't service interactive password/2FA prompts); App Store Connect app record created (ASC App ID `6793554196`, `eas.json`'s `submit.production.ios.ascAppId`); App Store Connect API Key generated and configured for fully non-interactive `eas submit` (`ascApiKeyId`/`ascApiKeyIssuerId` committed — safe, they're identifiers not secrets — the actual `.p8` private key lives outside the repo at `~/.eas-credentials/`, `chmod 600`, never committed). `app.json` also gained `ITSAppUsesNonExemptEncryption: false` (the app only does standard HTTPS client-side; BYOK's AES-256-GCM encryption runs server-side in a Supabase Edge Function, never compiled into the binary) to skip the export-compliance prompt on every build.
  - **Bug #1 — `npm ci` failed on EAS's cloud build**: `package-lock.json` had drifted from `package.json` (missing `react-dom`/`scheduler` entries) — `npm install` tolerates this with a warning, EAS's strict `npm ci` does not. Root cause traced further: `expo-router` transitively depends on web-only libs (`@radix-ui/react-tabs`, `vaul`) that declare `react-dom` as a non-optional peer dependency; without `react-dom` explicitly installed, npm resolved it to the latest version (19.2.8), which conflicts with this project's pinned `react@19.1.0`. Fixed via `npx expo install react-dom react-native-web` (picks the SDK-54-matched `react-dom@19.1.0` exactly) — this happens to also close the old "`npx expo start --web` environment gap" noted in this file for months, since those packages were never actually installed despite being suggested.
  - **Bug #2 — Apple Health silently disabled in every build ever made, dev or production**: `lib/apple-health.ts`'s `getHealthKit()` read `require('react-native-health').default`, but that package exports via plain `module.exports = HealthKit` — no `.default` property exists on it at all. Accessing `.default` on it doesn't throw, it just silently returns `undefined`, so `isAppleHealthSupported()` always returned `false` and the app always showed "Apple Health needs an iOS development build" — a message that turned out to be simply wrong; this bug predates this session and has evidently never been caught because no one had reached this point in real on-device testing before now. Fixed by dropping `.default`.
  - **Bug #3 — the *real* device then said "Apple Health is not available on this device"**: turned out `initAppleHealth()`'s inner `.catch(() => false)` around the `isAvailable()` check was silently swallowing a real thrown error (`kit.isAvailable is not a function`) into the same generic message as a legitimate "unsupported device" response. Removed the silent catch so the real error surfaces via `Alert.alert` (already wired in `gym.tsx`) — this turned an unfalsifiable bug report into an actual diagnosable one on the very next build. The real error: `react-native-health` is an old-architecture-only native module (podspec still v1.7.0-era, no Codegen/TurboModule support), and Expo SDK 54 defaults every project to the New Architecture, under which legacy `RCTBridgeModule`s that haven't been updated can register but fail to expose their methods to `NativeModules` at runtime. Fixed with `"newArchEnabled": false` in `app.json` — Expo's own supported opt-out for exactly this scenario; every other native dependency in this project (Expo's own modules, `react-native-ble-plx`, `react-native-svg`) supports the legacy architecture fine, so this was a safe, config-only fix requiring no library patching.
  - **Result**: build 8 (the New-Architecture-disabled build) confirmed working on a real iPhone via TestFlight — Apple Health steps sync now genuinely functions, the first concrete "built AND verified" result since the 07-07 screen-split pass. `tsc`/`vitest` held at baseline throughout (70/70 tests pass; the pre-existing Deno-vs-Node false-positive count unchanged).
  - **Process note for future sessions**: this sandboxed environment can run `eas build`/`eas submit` fine non-interactively once credentials exist, but cannot complete first-time interactive Apple ID/2FA login, nor can it reliably watch an `eas submit`'s long "waiting for Apple to process" phase to completion (background commands here get killed before that finishes) — check submission status via `eas build:view <id> --json`, or just have the user check TestFlight directly, rather than re-running `submit` repeatedly (which risks duplicate uploads of the same build — harmless, Apple just ignores the duplicate, but wasteful).
  - **Not yet done**: task 043 (FamilyControls entitlement application) — filed by the user separately from this thread, needs its submission date logged here once confirmed. The rest of the Stage-1 on-device checklist below (onboarding, habits, sleep, activity, calorie, etc.) is still open — Apple Health was only the first thing tested, not the whole pass.
- 2026-07-16 — **Task 061 closed** (code only, done solo while user was away from a device — second pick off the vault's own priority list, same session as task 060 below). `updateUserContextSummary()`'s `profile_md` was snapshot-only ("Tasks tracked: N") with no trend, per an open acceptance criterion. New `supabase/functions/_shared/trends.ts` (pure, zero Deno/Supabase imports, vitest-tested): `computeSleepTrend()` compares avg sleep over the most recent ~7 *logged* nights vs the ~7 before that (count-based windows so gaps in logging don't corrupt the comparison), returns "trending up/down — Xh vs Yh" or "steady around Xh" text, or `null` when either window has fewer than 4 real nights — never fabricates a trend from sparse data, same honesty rule as `bodyFormulas.ts`'s existing `null`/`—` pattern. Wired in via a dedicated 14-day `sleep_logs` query (deliberately NOT reusing `ctx.raw.sleep_logs`, which `buildContext.ts`'s own SLEEP block windows to only 7 days — too narrow for a recent-vs-prior comparison). 6 new vitest cases (`_shared/trends.test.ts`) — first test coverage for any `supabase/functions/_shared/` code; caught a real bug in my own first draft (test data spanned non-contiguous day-offsets, so the two 7-count windows didn't line up with the intended calendar weeks — fixed by making the fixture offsets contiguous, not by changing the trend function). Journal/therapy criterion trivially satisfied, same reasoning as task 060's `search_vault` — this function never queries those tables. `tsc` +1 over the previous 106 baseline, confirmed via `git stash` diff to be the same Deno-`.ts`-import false-positive pattern (one more import line = one more of the same non-error), not a real regression; `vitest` 70/70 pass. **Not deployed** — ships in the same pending `daily-briefing` redeploy as task 060's re-summarization work.
- 2026-07-16 — **Task 060 closed** (code only, done solo while user was away from a device — P6's on-device gate is unreachable remotely, so picked the next code-only item off the vault's own priority list instead). Investigated both acceptance criteria before writing anything: `search_vault` turned out to already be satisfied, just not as an explicit actionExecutor action — `buildContext.ts`'s `want('vault')` branch already does FTS over `vault_files` keyed on the user's message for any companion with `'vault'` in `contextSources` (life/schedule + work/focus today). That's the *correct* shape given this codebase's action model: `processActions()` in `_shared/actionExecutor.ts` runs strictly after the model's completion finishes, so an explicit `search_vault` action's results could never feed back into that same turn's response — pre-fetching into context before the Claude call is the only way a vault search actually grounds an answer. The real gap was the other criterion: `remember_about_user` (task 040/B4) only ever hard-capped `assistant_notes_md` at 30 bullets, silently dropping old facts rather than condensing them. Added `resummarizeAssistantNotes()` to `supabase/functions/daily-briefing/index.ts` — fires above a 7000-char (~1.75k token) threshold, condenses the notes block via Haiku into fewer, denser bullets, fire-and-forget alongside the existing `updateUserContextSummary` call, reusing `ctx.raw.user_context_summary` when already fetched. `tsc` held at the pre-existing baseline (106 errors before and after, confirmed via `git stash` diff — the "86" figure in this file's older entries is now stale, baseline has grown from other sessions' edge functions); `vitest` 64/64 pass, unaffected (this touches only edge-function code, no `lib/` pure modules changed). **Not deployed** — `daily-briefing` needs its own explicit redeploy instruction per the standing rule, same as the still-pending `ai-chat` v23→P3 redeploy.
- 2026-07-10 — **Code Audit v2 fix plan: P0 (hygiene) + P1 (nav restructure) + P2 (SharedContext) done.** Working from `SecondBrain/Projects/Habit Tracker/Build/2026-07-09 Code Audit v2 — Fix Plan.md`.
  - **P0**: pushed 3 stranded commits + the 2 untracked handover files; deleted a stale `log_meal unsupported` comment in `_shared/companions.ts` (it's wired); refreshed `current-state.md` against live `list_migrations`/`list_edge_functions` (confirmed all 28 migrations live, 6/7 functions deployed — `save-api-key` flagged as never deployed, a real gap the old prose had missed); filled the 3 zero-byte vault stub notes; wrote `[[Expo app open error]]` and marked the Kickstarter plan's device-testing blocker resolved; fixed the last UTC date bug in `ai-chat/index.ts` (`todayKey()` → `localDateKey(tzOffsetMinutes)` — staged, rides along with P2's redeploy, not deployed solo).
  - **P1 (task 078, resolved: enforce the 5-tab plan)**: `app/(tabs)/_layout.tsx` now registers exactly 5 visible tabs (Today/Habits/Health/Fitness/Life). Deleted `tree.tsx` (dead — its `bonsai` table was dropped from prod 07-06). New `app/(tabs)/health.tsx` and `life.tsx`: pure card-grid hubs, each card `router.push`es an existing screen (calorie/sleep/mood/steps/cycle for Health; goals/finance/library/focus-timer/calendar for Life) — no new business logic, gives the `life` companion its first real entry point (previously configured but unreachable, Code Audit v2 §1.5). `gym.tsx` retitled BODY→FITNESS; gained a RECORD ACTIVITY button (routes to the now-hidden `activity` tab) and wired its previously-dead header chart icon to `/progress`. Today's header cut from ~10 icons to 4 (search/calendar/profile/settings) — calorie/bluetooth/goals/finance/mood/library icons removed (now reachable via Health/Life/Settings); added a profile icon; moved sign-out onto the Profile screen; moved bluetooth into Settings → Connected Devices. `activity`/`progress`/`profile` stay registered in `_layout.tsx` with `options={{ href: null }}` (reachable via `router.push`, no tab-bar slot) rather than rebuilding any of their working internals. Verified every formerly-icon-reached route still has a live `router.push` call (grepped post-change). `tsc`/`vitest` held at the pre-existing baseline — no new errors, 32/32 tests still pass. Not verified on-device (routing-only change, no data risk, but still unverified like everything else this build). Full route mapping recorded in `tasks/078-nav-restructure.md`'s new Decision section.
  - **P2 (the "AI can't see other sections" fix)**: **B1** — new SharedContext block in `buildContext.ts` runs for every companion unconditionally (not gated by contextSources), appending a compact `SHARED (today): calories X/Y · protein X/Yg · sleep Xh · tomorrow: leg day · steps X · focus Xm this week · water X.XL` line, reusing `raw.*` a companion's own contextSources already fetched (zero extra queries for calorie/sleep/focus) and running one small extra query otherwise. **B2** — new `daily_steps` table (migration `029_daily_steps.sql`, **applied live** via Supabase MCP) + fire-and-forget upsert from `lib/body-data.ts`'s Apple Health merge path (`syncTodayStepsToSupabase`) — steps were previously local-only, invisible to every AI companion; added as an explicit contextSource on the `activity` companion. **B3** — new `lib/focus-data.ts`: `app/focus-timer.tsx` now writes `focus_sessions` + calls `postWrite('focus', …)` on every completed work interval (the app side never did this before, only the device did); added a `focus_sessions` buildContext handler (last-7-days total+count) and contextSource on the `focus` companion; added `'focus'` to `postWrite.ts`'s `Entity` union, incrementing `cumulative_stats.total_focus_secs`. **B4** — implemented `remember_about_user` in `_shared/actionExecutor.ts`: appends a deduped, 30-bullet-capped note to `user_context_summary.assistant_notes_md`; declared on `habitCoach`/`life`. Previously nothing ever wrote that column despite `buildContext` rendering it as "NOTES YOU'VE SAVED" — the AI could not remember anything a user told it to remember. **B5** — wired the previously-dead `water_logs` handler into the SharedContext block instead of deleting it (real data, written by `lib/body-data.ts`'s `addWater`, just never read by any companion). `tsc`/`vitest` held at baseline. **Migration applied live; `ai-chat` itself is NOT yet redeployed** — auto mode's safety classifier blocked the redeploy as an inferred (not explicitly user-named) production action. Code is committed and pushed; next session should either run `supabase functions deploy ai-chat` directly or explicitly ask for the deploy.
- 2026-07-07 — **Cal AI/Strava parity pass: real data + dedicated pages.** Prior research (`SecondBrain/Research/`) compared the calorie, run, and body/gym trackers against Cal AI and Strava. Two problems fixed together since they shared root causes:
  1. **Fake-data removal.** `lib/body-data.ts`'s `seedData()` (fabricated steps/training history, headline lifts, "weakest muscle," strength trend, sleep, protein — persisted on first launch, indistinguishable from real data after that) deleted entirely; new `lib/bodyFormulas.ts` holds the real, pure replacements (`trainingDayTypeFor`, `computeHeadlineLiftsFromData`, `computeLeastTrainedMuscleFromData`/`computeMuscleGroupTalliesFromData`, `computeStrengthTrendFromLifts`, `computeNextSessionFromPlan`), each backed by real `lib/workout-data.ts`/`lib/sleep-data.ts`/`lib/meals-data.ts` reads and an honest `null`/`—` when no real data exists. "1RM" renamed "TOP SET" (we track max logged weight, not a calculated 1RM). `lib/steps-data.ts`'s entirely separate fake run-tracking system (`Run`/`startRun`/`endRun`, fabricating pace at a hardcoded 9.6 km/h with zero GPS) deleted — it duplicated the real GPS tracker in `lib/activity-data.ts`; the STEPS screen's "start a run" button now routes to the real Activity tab, weekly/monthly goals are real rollups over saved activities. `lib/foodVision.ts`'s hardcoded 520-calorie mock-estimate fallback deleted — returns `null` on failure now, surfacing a blank editable form instead of invented numbers. New `lib/nutritionFormulas.ts`: real Mifflin-St Jeor `computeTargets()` from onboarding's age/sex/height/weight (falls back to the old constants, honestly framed as "assumed average adult," when those fields were skipped).
  2. **Screen restructuring** — Cal AI/Strava use one dedicated full-screen page per flow; this app had 3 mega-screens. `app/calorie.tsx` (493 lines) → `app/calorie/{index,capture,confirm,history}.tsx`. `app/(tabs)/gym.tsx` (856 lines) slimmed to a hub; new `app/strength.tsx` holds full lift/muscle/trend detail; `app/(tabs)/progress.tsx` (was a 17-line dead stub) rebuilt into a Strava-style profile/stats page (activity calendar, trophy case, all-time stats). `app/(tabs)/activity.tsx` is now record-only; **task 033 (post-activity summary), previously pending, is now done** — new `app/activity-summary.tsx` (real splits via new `computeSplits()`, real elevation profile, both vitest-tested) + new `app/activity-history.tsx`.
  3. **New Supabase Storage bucket** (`supabase/migrations/025_meal_photo_storage.sql`, private, RLS by `auth.uid()` folder) — meal photos now actually upload and survive a reinstall, instead of staying a local-only file URI. **Not yet applied** — see Action Needed below.
  4. **Background GPS recording implemented** (`lib/locationTask.ts`, `expo-task-manager`, `app.json` `UIBackgroundModes`/`expo-location` plugin config) — **explicitly unverified**, no device/EAS build/battery test this session; foreground-only tracking (previously verified) is the automatic fallback if background permission is denied.
  5. **New pure-module testing pattern**: any `lib/*-data.ts` file importing `./supabase` can't be unit-tested directly (React Native's own source uses Flow syntax vitest/rolldown can't parse) — pure computation now lives in sibling `lib/*Formulas.ts` modules with zero RN/Supabase imports (`bodyFormulas.ts`, `nutritionFormulas.ts`, `activityFormulas.ts`), and the `-data.ts` file re-exports it. **vitest added from scratch** — despite CLAUDE.md's claim, no test infra existed (`lib/classifier.ts` doesn't exist; no `vitest` dep or `test` script) — now `npm test` runs 32 passing tests across the 3 new formula modules, including empty-state/edge cases (0/1 PB entries, no doneLog, untouched muscle groups, negligible leftover distance).
  6. New shared components: `components/GymHeatmap.tsx`, `components/Spark.tsx` (both extracted from `gym.tsx`), `components/SummaryCard.tsx` (extracted from `activity.tsx`), `components/ElevationGraph.tsx` (new).
  7. `lib/postWrite.ts` gained `getCumulativeStats()` (a read-side companion to its existing write-side `incrementCumulativeStats`) for the new profile/stats page.

  `npx tsc --noEmit` held at the pre-existing 86-error baseline throughout (all Deno/edge-function/unrelated-component false positives, none touched by this pass — verified via `git stash` diff before starting). **Could not run `npx expo start --web`** to visually smoke-test — `react-dom`/`react-native-web` were never installed in this environment (pre-existing gap, not caused by this pass); verification for this session is `tsc` + `vitest` + code review only. **Nothing has been run on a real device or applied to the live Supabase project** — see Action Needed below.

- 2026-07-06 — **AUDIT FIXES.** Ran a full plan/code/AI-system audit using Fable 5 across 3 parallel reviewers (architecture, code quality/data layers, AI companion + fan-out system), then worked through its findings in priority order. In commit order:
  1. **Committed the untracked AI core** — `ai-chat/`, `_shared/actionExecutor.ts`, `lib/streaks.ts` (imported by already-tracked `postWrite.ts` — a fresh clone could not have worked), the BLE bridge, migrations 006/008, etc. had been live in production since 2026-06-29 without ever being in git.
  2. Added `supabase/migrations/APPLIED.md` (a real ledger of which migrations have run against prod — previously only scattered prose) + a standing "never deploy uncommitted code" rule in CLAUDE.md.
  3. **Fixed a critical streak-corrupting bug**: `new Date(dateKey)` parses as UTC midnight but `toDateKey` formats local — every backward-walk day-decrement (`computeStreak`, `computeChallengeStreak`, `computeFrozenDates`, `courseProgress`) silently stepped TWO local calendar days in any negative-offset timezone (all of the Americas). A perfect month-long streak read as 1-2 for those users. Added `lib/dateKey.ts`'s `addDaysToKey`/`daysBetweenKeys` and replaced every raw `new Date(key)` arithmetic site in habits/sleep/mood/medications-data.ts.
  4. **Fixed an onboarding dead-end**: when Supabase requires email confirmation, the account screen had no session yet and redirected to `/(auth)/login` — but the root routing effect bounced the user straight back to `/(onboarding)/welcome` since the local onboarding-done flag was still false, with the only escape ("I already have an account") deleting all pending answers. Fixed by checking `inAuth` first and never redirecting away from it. Also fixed a double-flush race (`flushOnboardingIfNeeded` now has an in-flight-promise guard) that could create a duplicate first habit and silently drop the briefing-module choice.
  5. **New `lib/storageLock.ts`** generalizes the single-key lock `lib/task-sync-lock.ts` already used for `@tasks` into a per-key version, wired into every mutating function across 12 domain data layers — closes a whole class of "two concurrent writes to the same AsyncStorage blob, second one silently discards the first" races. Also fixed `advanceDate`/`predictNextPeriod` (same UTC-parse-then-local-mutate bug as #3) and `markDoneToday`'s Supabase upsert (no `onConflict` target, so it silently failed on a second same-day sync instead of resolving against the real unique index).
  6. Fixed `lib/badges.ts`'s exact-equality threshold checks (`=== 1`/`=== 10` → `>= 1`/`>= 10`) — a count that skipped past N (e.g. a habit synced after the fact) permanently missed the badge.
  7. **Fixed the same UTC/local bug in the server AI layer** — `_shared/buildContext.ts`/`actionExecutor.ts` computed TODAY, the gym_plan "tomorrow" line, and today/tomorrow date resolution via UTC, reporting the wrong calendar day for anyone west of UTC from evening onward (the flagship "tomorrow is leg day" feature would name the wrong day; voice-created tasks landed on the wrong date). New `_shared/localDate.ts`, driven by a `tzOffsetMinutes` the client now sends (`ChatScreen.tsx`, `BriefingCard.tsx`, `lib/ble-bridge.ts`).
  8. **Decoupled precomputed flags from contextSources** — flags used to only compute from data a companion's own `contextSources` had already fetched, so "cross-domain" flags almost never actually crossed domains (OVERREACHING could only ever reach gym/goals, never calorie). Each flag now has its own fixed, tiny query, reusing already-fetched data when available.
  9. **Rate-limited daily-briefing** (previously zero limit — an open cost hole) via a new atomic `increment_briefing_usage` RPC checked before any buildContext/Claude call runs. Fixed `ai-chat`'s `api_usage` upsert, which was overwriting the day's token totals with just the latest message's tokens instead of accumulating (new `increment_api_usage` RPC). Changed `FREE_DAILY_CAP`'s default from 500 (a dev convenience left live in prod) to the production 25/day system-model.md specifies.
  10. **Companion picker UI** — the audit's highest-leverage single fix: `ChatScreen.tsx` hardcoded `companionType: 'habitCoach'` everywhere, so 6 of 9 companions (calorie/activity/sleep/goals/mood, all already wired into buildContext) had no chat UI at all, and task 038 (verify 5 companions end-to-end) was literally unrunnable. Added a `companionType` prop + wired entry points into `app/calorie.tsx`, `app/(tabs)/activity.tsx`, `app/modals/sleep-detail.tsx`, `app/(tabs)/gym.tsx`, `app/modals/goals.tsx`, `app/modals/mood.tsx` — 7 of 9 companions now reachable. `focus` (large, orientation-sensitive screen) and `life` (no distinct screen yet) deliberately left unwired rather than risk an edit to unfamiliar code without device testing.

  tsc clean throughout (baseline crept from 66 → 69 pre-existing/expected errors, all the same Deno-vs-Node false-positive pattern from new `.ts`-suffixed imports, not new real errors — checked after every change). **None of this has been run on a device or redeployed.**
  11. **Fan-out cleanup**: `lib/streaks.ts` rewritten from a dead-wrong generic per-entity streak cache (habits bypass it entirely; ignored update/delete actions; had a same-day early-return bug) down to the one real job it did — `refreshWorkoutsTotal`, unconditional, correct. `updateUserContextSummary` (regenerated a 5-line summary via a 130-row fetch on EVERY domain write) moved out of `postWrite.ts` into `daily-briefing/index.ts`, where it now runs once per briefing instead of once per write.
  12. **Wired `log_meal` + BYOK key consumption + companion personas into `ai-chat`**: `log_meal` (calorie's only action) always returned `'unsupported'` — now inserts into `meals`. The BYOK Settings toggle encrypted and stored a user's own Anthropic key but `ai-chat` always used the shared app key regardless — new `_shared/byok.ts` (extracted from `save-api-key/index.ts`'s encrypt logic) adds `getUserApiKey()`, now consulted before falling back to `ANTHROPIC_API_KEY`. `companion_personas` (per-user name/nickname from Settings) was written but never read back — now fetched and substituted into the system prompt instead of always using `cfg.defaultName`. Also: `reschedule_task`/`complete_task` used plain `update()` calls that report no error on 0-rows-affected, so a hallucinated `taskId` silently "succeeded" doing nothing — both now select the row back and throw if missing. Deliberately NOT done: replacing the numeric confidence gate with categorical intent labels (needs prompt changes across every companion, too large to do blind) — instead just removed the misleading raw `73%` display in `ChatScreen.tsx`'s PreviewCard (replaced with a flat "NEEDS REVIEW" label), since that number is model self-reported, not calibrated.
  13. **`tasks/078-nav-restructure.md` written** (not implemented) — records the gap between system-model.md's canonical 5-tab decision and the shipped 7 tabs + assorted standalone screens, with a candidate route mapping, so the next session can pick (enforce vs. formally revise) instead of re-discovering the drift. **Amended `database.md`'s canonical rules**: pushed back on the audit's "new tables should use native `DATE` columns" suggestion — that would reintroduce the exact UTC/local mismatch bug class fixed in this session, so TEXT-typed canonical date keys stay the rule, explicitly documented as deliberate now. Did adopt the audit's id-generation point in a narrower form: flagged (not yet consolidated) that 13 files each redefine their own weak `genId()` instead of sharing one `crypto.randomUUID()`-backed helper — left as a follow-up task since it touches every domain data layer's write path.

  Everything in this block is tracked as done-but-unverified — no device/simulator access this session. Nothing further remains from the original 3-agent audit's fix list; only nav-restructure's actual implementation (078) and the `genId()` consolidation are open follow-ups it surfaced.
- 2026-07-05 — task/025 done (built solo, no device/simulator available): PPL day planner. New `getGymPlan()`/`setGymPlanDay()` in `lib/workout-data.ts` (reads/writes the existing `gym_plan` row, one column per weekday, reusing `BodyMovement`'s push/pull/legs/upper/lower vocabulary plus new 'rest'/'cheat' string values — no schema change). New WEEK PLAN row in `app/(tabs)/gym.tsx`, tap-to-cycle. **Found and fixed a real gap**: `markDoneToday()` (marks a workout session done) had never called `postWrite` at all since it was written — writes went straight to `workout_done_log` with zero fan-out, meaning `cumulative_stats.total_gym_sessions` (task 014) and the `first_workout`/`workouts_10` badges (task 063) never actually fired despite both being built against this call site. Now wired. tsc clean. Not verified on-device.
- 2026-07-05 — task/057 done: `supabase/migrations/023_vault_files.sql` (schema only — `vault_files` with GIN FTS index on content, RLS, `source` CHECK constraint). Nothing writes to it yet; the actual Obsidian sync client (task 059) needs iCloud file access and is device-gated.
- 2026-07-05 — tasks/040+041 done (built solo, no device/simulator available): Precomputed flags + cross-domain context wiring. Extended `_shared/buildContext.ts` with 5 flags (OVERREACHING, SLEEP_DEBT, UNDERFUELLING, LOW_PROTEIN, STRESS_SLEEP), each computed from whatever `raw.*` a companion's own `contextSources` already populated — no flag triggers an extra query. `meals`'s query extended to also fetch `nutrition_targets` + `protein_g` (previously not selected). `BASE_PERSONA` (shared by every companion) now explains each flag. Calorie AI gained `gym_plan` as a context source (new `want('gym_plan')` block reports tomorrow's session); Sleep AI gained `mood_logs` as a stress-proxy stub until wearables land. tsc clean. Not redeployed or verified on-device.
- 2026-07-05 — task/074 done (built solo, no device/simulator available): Global Search. `components/GlobalSearch.tsx` indexes local AsyncStorage stores directly (habits, goals, library's 4 sub-types, finance's expenses/bills, workout templates, tasks) — offline-capable, consistent with the app's local-first pattern. Never reads `journal_entries`/`therapy_notes`/mood content at all (structurally unsearchable, not filtered after the fact — those tables hold zero plaintext per task 066). Goals and tasks deep-link to the specific record (`app/modals/goals.tsx` gained a `highlightId` param to auto-expand the matched goal; tasks reuse `calendar/day`'s existing `date` param); habits/library/finance/workouts navigate to the section only — flagged as partial rather than claimed complete. New magnifying-glass icon next to "TODAY" (kept separate from the other action icons, already flagged as overcrowded). tsc clean. Not verified on-device.
- 2026-07-05 — task/076 done (built solo, no device/simulator available): Streak freezes, MVP-only (repair tokens/holiday mode explicitly deferred per the task's own text). Extended `lib/habits-data.ts` (not `lib/streaks.ts` — per-habit streak logic already lives there, see tasks/023): new `auto_freeze_enabled` per-habit flag (migration `022_streak_freezes.sql`, one column), `computeFrozenDates()` (walks chronologically, caps at 2/month, resets on calendar-month boundaries), `computeStreakWithFreezes()` (reuses the existing streak algorithm by feeding it synthetic frozen-day entries rather than duplicating the logic). `HeatmapCalendar` gained a blue 'frozen' cell state. `app/(tabs)/habits.tsx` gained a per-habit auto-freeze toggle. Push notification on freeze use is NOT implemented — no notification infra, same gap as task 063's badges. tsc clean. Not run live or verified on-device.
- 2026-07-05 — task/064 partially done (built solo, no device/simulator available): Library, manual entry only. `supabase/migrations/021_library.sql` (books/movies/saved_links/ideas; `reading_stats`/`movie_stats` rollups skipped, nothing reads them yet). `lib/library-data.ts`: per-type add/status functions, plus `classifyCapture()` — a plain heuristic (URL regex, then keyword matching, then a length-based default), explicitly NOT the Google Books/TMDB-backed classification the task describes (no API keys exist for either service this session). `app/modals/library.tsx`: Books/Movies/Links/Ideas segmented toggle (not a tab — same reasoning as Finance/Settings/Sleep/Goals) with a single natural-language capture bar that routes a paste to the right list. No TMDB attribution notice added — showing one while displaying zero TMDB data would misrepresent the screen; an honest "not wired up yet" note is there instead. New book icon on the Today header, now 9 icons — flagged repeatedly this session, a real redesign (e.g. an overflow sheet) is overdue. tsc clean. Not run live or verified on-device.
- 2026-07-05 — task/067 partially done (built solo, no device/simulator available): Cycle tracking, opt-in only. `supabase/migrations/020_cycle.sql` (cycle_logs, cycle_settings — standard per-user RLS; "stricter RLS" beyond row ownership isn't a real Postgres primitive, so the real strictness is app-level). `lib/cycle-data.ts`: opt-in flag gates everything, average-cycle-length next-period prediction, and `addLog()` deliberately skips `postWrite()` so cycle data never touches cumulative_stats/badges/context-summary. `app/modals/cycle-tracking.tsx` shows only an opt-in screen until turned on, reachable via a new Settings row ("Cycle tracking, off by default") — no icon anywhere else, satisfying "hidden by default". No `cycle`/`cycle_logs` entry exists in `_shared/companions.ts` or `_shared/buildContext.ts` at all, trivially satisfying "absent from buildContext unless opted in." Face ID gate NOT implemented — no `expo-local-authentication` dependency, no device to verify a biometric flow, same reasoning as task 066's encryption gap. tsc clean. Not run live or verified on-device.
- 2026-07-05 — task/066 partially done (built solo, no device/simulator available): Mood log (the task's own "MVP-value part — build that fully"). `supabase/migrations/019_mental_health.sql`: `mood_logs` ships with real reads/writes (plaintext by design — the client-side-encryption privacy rule names journal/therapy specifically, not mood); `journal_entries`/`therapy_notes` created as SCHEMA ONLY, matching database.md exactly, with zero application code touching them. Reasoning: no crypto dependency is installed in this project and there's no device to verify one works — the task's own gate ("before a single field ships") makes shipping nothing the honest call over guessing at working encryption for a mental-health journal. `lib/mood-data.ts` + `app/modals/mood.tsx`: 1-10 mood/stress scales, trigger tags, note, 14-day sparkline. New `mood` companion, `contextSources: ['mood_logs', 'user_context_summary']` only — never touches journal/therapy, satisfying that acceptance criterion trivially since nothing exists there to leak. Reachable via a new emoticon icon on the Today header (now 8 icons). tsc clean. Not run live or verified on-device.
- 2026-07-05 — task/065 done (built solo, no device/simulator available): Finance tracker, manual only (bank connection stays with task 051). `supabase/migrations/018_finance.sql` (expenses/bills/budgets, RLS on all three). `lib/finance-data.ts`: month-scoped expense list, recurring bills with a "mark paid → auto-advances due date by frequency" flow, per-category budget targets, `budgetStatus()` flags over-spend. `app/modals/finance.tsx`: Spending/Bills/Budgets segmented toggle (not a tab — same reasoning as Settings/Sleep/Goals this session), reachable via a new cash icon on the Today header. Noted but not fixed: that header now has 7 icons and is genuinely getting crowded. tsc clean. Not run live or verified on-device.
- 2026-07-05 — task/068 done (built solo, no device/simulator available): Goals domain, structured part (vision board is optional polish per the task itself, not built). `supabase/migrations/017_goals.sql` (goals/milestones/goal_logs, RLS on all three). `lib/goals-data.ts`: progress % derived from milestone completion, falling back to the latest manually-logged percent for goals with no milestones yet. `app/modals/goals.tsx`: expandable goal cards, milestone checklist, add-goal flow. New `goals` companion + a `want('goals')` block in `_shared/buildContext.ts` (lists active goals with milestone completion %, alongside `habit_logs`/`workout_done_log` per the task's own acceptance criterion). Added `'goal'` to `lib/postWrite.ts`'s `Entity` union. Entry point: new flag-checkered icon on the Today header. tsc clean. Not deployed or verified on-device.
- 2026-07-05 — tasks/014+063 done (built solo, no device/simulator available): Badge launch set + a long-standing postWrite gap fixed. `lib/badges.ts` (new): 10-badge launch catalogue, reads raw AsyncStorage blobs directly (not the domain `lib/*-data.ts` modules — would create a circular import back through `postWrite.ts`). Extended `lib/habits-data.ts` and `lib/sleep-data.ts` call sites to pass the fields badge checks need (habit streak, sleep wake time, Phone Down Challenge streak — the latter previously never called `postWrite` at all). **Found and fixed a real race**: all 6 `postWrite` fan-out effects ran in one `Promise.allSettled`, but the new badge check needs streak state a sibling effect (`updateStreak`) had just written, with no guaranteed order — `updateStreak` now runs first, awaited on its own, before the rest run in parallel. **Also closed a gap that predates this session**: `incrementCumulativeStats` (task 014, supposed to be "live" since day one) was still a `console.log` stub because the `cumulative_stats` table had never been created — added `supabase/migrations/016_cumulative_stats.sql` (+ `015_badges.sql` for `badges_earned`) and real increment logic. Gave the `profile.tsx` stub a real badge grid (hidden badges show `???`), leaving the rest of that screen a placeholder (full build is task 070). Push notification on badge unlock is explicitly NOT implemented — no notification infra exists yet. tsc clean. Not run live or verified on-device.
- 2026-07-05 — task/020 done (built solo, no device/simulator available): minimal Settings. `app/settings/index.tsx` (API-key toggle) + `app/settings/companion-persona.tsx` (per-companion name/photo, upserts the already-live `companion_personas` table). New `supabase/functions/save-api-key/` encrypts a user-provided Anthropic key with AES-256-GCM (Web Crypto, server-only `API_KEY_ENCRYPTION_SECRET`) before writing ciphertext to new `supabase/migrations/014_user_api_keys.sql` — plaintext never persisted, never returned to the client. Deliberately NOT built as `app/(tabs)/settings.tsx` per the task file — that predates system-model.md's "Profile via header icon" nav decision and would've made an already-crowded tab bar (7 tabs after this session's additions) worse; it's a modal route behind a new gear icon in the Today header instead. Flagged clearly in task notes: the saved BYOK key is stored but NOT yet consumed by `ai-chat`'s actual model call (that's real follow-up work, out of this "minimal" task's scope). tsc: client files clean; the new edge function has the same Deno-vs-Node false-positive pattern as the other three functions. Not deployed or verified on-device.
- 2026-07-05 — task/062 done (built solo, no device/simulator available — the highest-risk build of the session): Onboarding flow, 10 screens (`app/(onboarding)/welcome|basics|goals|targets|first-habit|skills|book|connect|account|briefing-builder.tsx`), sharing `components/OnboardingShell.tsx` for the progress-dots/title/CTA chrome. Screens 1-8 collect everything into one local AsyncStorage blob (`lib/onboarding-data.ts`) — no Supabase write happens before account creation (screen 9, `supabase.auth.signUp()`). Added `supabase/migrations/013_user_profiles.sql` (`user_profiles` didn't exist despite being scoped to task 006 — `006_ai_companions.sql` never created it) and `lib/featureFlags.ts` (didn't exist yet either — one of system-model.md's 5 governors; `healthKitConnect: true`, everything else `false`). `flushOnboardingIfNeeded()` writes `user_profiles`/`nutrition_targets`/first habit/`briefing_preferences` once a session exists and is called from both the briefing-builder finish button and `app/_layout.tsx`'s session effect (covers the email-confirmation-required path, where the session only appears on a later login). Extended `app/_layout.tsx` with a local `@onboarding_complete` flag, independent of login state, to decide whether a logged-out user lands on `/(onboarding)/welcome` or `/(auth)/login`. Caught and fixed a real bug before committing: `signUp()` can make `session` truthy immediately (email confirmation off), which happens on screen 9 — one screen before the flow ends — so the root layout deliberately does not auto-redirect out of `(onboarding)` on session alone (only from `(auth)`), letting the user reach screen 10 first; that screen calls `router.replace('/(tabs)')` itself once the flush finishes. Also caught a stale-state bug in the "I already have an account" skip path (the onboarding-flag check now re-reads on every top-level segment change, not just once on launch). Two interpretation calls documented in tasks/062 notes ("skills" and "book" screens — the master spec isn't in this repo to check verbatim). tsc clean (52 total, same pre-existing pattern as before — daily-briefing's Deno false-positives account for the +7 over the earlier 45). **Not verified on-device at all** — this rewrites the auth-entry routing, the single most important thing to test before trusting it.
- 2026-07-05 — tasks/018+019 done (built solo, no device/simulator available; not deployed): Daily Briefing, the last missing piece of system-model.md's "lean MVP spine". `components/BriefingCard.tsx`: reads `@habittracker_briefing_{date}` from AsyncStorage on mount for an instant cached render, empty state ("No briefing yet today" + GET DAILY BRIEFING button) before the first generation, manual refresh via `supabase.functions.invoke('daily-briefing', ...)`. Mounted on the Today screen (`app/(tabs)/index.tsx`) directly below the header — a 2-line addition to keep the footprint small in an already-large file. `supabase/functions/daily-briefing/index.ts` (new): reads `briefing_preferences.selected_modules[]` (defaulting to `['tasks','habit_logs']` when unset — onboarding's briefing-builder screen, task 062, doesn't exist yet) and passes them straight through as `buildContext`'s source filter — a module the user hasn't selected is never queried, satisfying the privacy/cost acceptance criterion via the same mechanism `ai-chat` already uses. Single one-shot Haiku call (no conversation history, no actions, no per-message rate-limit — this isn't a chat turn) with an explicit "under 150 words" system-prompt instruction and a `max_tokens: 300` ceiling. tsc clean (only the same Deno-vs-Node false-positive pattern as `ai-chat`/`food-vision`, not real errors). Not deployed (`supabase functions deploy daily-briefing` still needed) or verified on-device.
- 2026-07-05 — task/037 done (built solo, no device/simulator available; not redeployed): extended `supabase/functions/_shared/buildContext.ts` with windowed queries for `habit_logs` (per-habit completion counts, last 14 days, joined against the user's active habits), `meals` (today's calorie total + a 3-day recent list), `activities` (last 2 weeks, type/distance/duration), and `sleep_logs`/`sleep_phone_logs` (avg/latest hours, latest Phone Down Challenge result + streak) — every new query windowed (7-14 day lookback or a small `.limit()`), matching the file's existing pattern. Added 3 new companion configs to `_shared/companions.ts` that didn't exist yet (`calorie`, `activity`, `sleep` — 3 of the 14 in system-model.md's roster, added now that their domains are live) and gave `habitCoach` the new `habit_logs` source. No chat entry point wires to the 3 new companions yet — `components/ChatScreen.tsx` still hardcodes `companionType: 'habitCoach'` — that's separate UI-wiring scope. Not redeployed (`supabase functions deploy ai-chat` still needed).
- 2026-07-05 — tasks/031+032 partially done (built solo, no device/simulator available): Activity (hike/run/walk) domain. `supabase/migrations/012_activity.sql` (activities, activity_stats_cumulative — RLS on both; trail-database-only columns on the stats table intentionally omitted per task 031's own scope). `lib/activity-data.ts`: haversine distance, elevation gain from altitude deltas, pace calc, per-segment pace array (for route colouring), GeoJSON `LineString` export (correct `[lng, lat]` order), and a cumulative-stats bump per activity type. `app/(tabs)/activity.tsx`: new ACTIVITY tab, type selector, `Location.watchPositionAsync` FOREGROUND-only tracking (explicitly not background — that needs `startLocationUpdatesAsync` + a background task, deferred until device-testable), live duration/distance/pace, an abstract SVG polyline (no map library installed; colour-coded per segment by relative pace) standing in for a real map, and a recent-activities list. Saves go through `postWrite('activity', …)` (added to the `Entity` union, same pattern as `'meal'`/`'medication'`). The task's background-location + 30-min battery-drain acceptance criterion is explicitly left unchecked — it's a physical measurement, not something to claim without a device. tsc clean for all new/changed files. Not verified on-device — needs migration `012` run live, then a real walk with location permission granted.
- 2026-07-05 — tasks/035+036 done (built solo, no device/simulator available): Sleep domain. `supabase/migrations/011_sleep.sql` (sleep_logs, sleep_phone_logs, winddown_logs — RLS on all three; winddown_logs has no screen yet, no task builds one). `lib/sleep-data.ts` (local-first, mirrors `lib/habits-data.ts`): manual nightly log with `computeTotalHours()` handling the overnight wrap, and the Phone Down Challenge (`scoreChallenge()` — pass/close/fail against a user-settable target time, `computeChallengeStreak()` — its own consecutive-pass counter, deliberately separate from `lib/habits-data.ts`'s streak logic since these are different domains). `app/modals/sleep-detail.tsx`: log-a-night card (bedtime/wake/star-rating quality), Phone Down Challenge card (target time + manual phone-down-time entry, since real iOS Sleep Focus detection needs native Screen Time/Shortcuts integration — task 042, device-gated, not attempted here), and a small local bar chart for the trailing week. Registered as a new modal route (`app/_layout.tsx`, `presentation: 'modal'`) and wired as the entry point behind the existing SLEEP recovery card on the Body hub (`app/(tabs)/gym.tsx`, previously a static, non-interactive display). tsc clean for all new/changed files. Not verified on-device — needs migration `011` run live, then a manual log + Phone Down Challenge check.
- 2026-07-05 — tasks/034 reviewed, no code change: `app/(tabs)/gym.tsx` (already tab-titled "BODY") turned out to already be the unified Body hub this task asked for — interactive water/weight logging into the correct reconciled tables, a steps ring with local fallback + optional HealthKit sync, workout preview. Writing a separate `app/(tabs)/body.tsx` would have duplicated a working 791-line screen for no functional gain, so instead corrected the stale "Gym" vs "Body (partial)" split in the table above (leftover from before tasks/005's reconciliation) and marked all applicable acceptance criteria met in tasks/034.md. The one unmet criterion (sleep/cycle/mood modal wiring) is correctly N/A — those domains don't exist yet.
- 2026-07-05 — tasks/024 done (built solo, no device/simulator available): Medication & Supplements sub-section of the Habits screen. `supabase/migrations/010_medications.sql` (medications, medication_logs — RLS on both, next free number was 010; `cycle_linked`/`cycle_day` columns included to match database.md's canonical schema but unused until task 067). `lib/medications-data.ts` mirrors `lib/habits-data.ts`'s pattern; generalized `computeStreak()` in `lib/habits-data.ts` to accept `{date, completed}[]` instead of `HabitLog[]` so medications' `{date, taken}` logs reuse the same streak algorithm via `computeMedStreak()` — avoided a duplicate implementation. Added `computeAdherence30d()` (% of trailing 30 days taken, capped to the medication's actual age) and `courseProgress()` ('Day X of Y' when `courseLength` is set). Extended `lib/postWrite.ts`'s `Entity` union with `'medication'` (same pattern as tasks/028's `'meal'` addition). `app/(tabs)/habits.tsx` gained a HABITS/MEDICATION segmented toggle (not a new tab, per this task's spec) and a second add-flow modal. tsc clean for every new/changed file. Not verified on-device — needs migration `010` run live, then a manual add/dose/adherence check.
- 2026-07-05 — tasks/022+023 done (built solo, no device/simulator available): Habits tab. `supabase/migrations/009_habits.sql` (habits, habit_logs, streak_data, streak_events — RLS on all four, next free number was 009 not the task file's hardcoded 004). `lib/habits-data.ts` (local-first, mirrors `lib/meals-data.ts`'s pattern): habits + habit_logs cached in AsyncStorage, fire-and-forget to Supabase, per-habit streak computed straight from `habit_logs` (deliberately not the generic `lib/streaks.ts` cache, which only holds one counter per entity *type* and would collide across multiple habits) and cached into `streak_data`. `components/HeatmapCalendar.tsx` (green/red/grey cells, freeze/holiday colours deferred to task 074 per spec). `app/(tabs)/habits.tsx`: list, per-habit streak text, completion toggle, long-press-to-delete, add-habit modal (name + daily/weekly). Registered as a new `HABITS` tab in `app/(tabs)/_layout.tsx` between Today and Body — the other four tabs untouched (incremental restructuring, not the system-model.md big-bang 5-tab rename). tsc clean for every new/changed file. Not verified on-device — needs migration `009` run live, then a manual add/complete/restart check.
- 2026-06-22 — tasks/001 done: canonical docs (system-model.md, database.md, current-state.md) created, CLAUDE.md rewritten, tasks/002–077 generated.
- 2026-06-22 — tasks/002 done: supabase/migrations/001_baseline.sql created from the live schema (run-this-once.sql). Discovered workout-schema.sql defines a conflicting, never-wired-up schema — marked dead rather than folded in. All five loose .sql files marked superseded in-place. Migration not yet executed against the live Supabase project (no DB credentials/CLI in this session) — run it manually in the SQL editor.
- 2026-06-22 — tasks/003 done: found 5 independent inline re-implementations of the old "YYYY-M-D" date key (app/(tabs)/index.tsx, lib/task-schedule.ts, lib/body-data.ts, lib/apple-sync.ts, lib/workout-data.ts), affecting tasks.date / workout_done_log.date / pb_log.date in Supabase plus @tasks and BodyData.stepsHistory in AsyncStorage. Plan: consolidate into lib/dateKey.ts, one-time rewrite (no dual-format support — no production users yet), SQL migration for Supabase columns + a guarded boot-time AsyncStorage migration. Execution is tasks/004.
- 2026-06-22 — tasks/004 done: added lib/dateKey.ts as the single canonical implementation; found and fixed a 6th call site (app/calendar/index.tsx) missed by tasks/003's inventory; wrote lib/migrateDateKeysV2.ts (one-time AsyncStorage migration, wired into app/_layout.tsx ahead of the splash-screen hide and auth redirect) and supabase/migrations/002_date_key_format.sql (Supabase column rewrite, not yet executed live). Used plain Date math instead of adding date-fns, to avoid touching package.json's already-dirty state for no functional gain. tsc clean (28 pre-existing errors, unrelated, unchanged). No on-device visual verification possible this session.
- 2026-06-22 — tasks/005 done: decided to extend the existing gym/body tables additively rather than introduce parallel spec-named tables (workouts/personal_bests/body_logs) — documented in database.md with the full mapping. Drafted supabase/migrations/003_gym_body_reconcile.sql (workout_done_log gains duration/GPS/HR/calories/notes, pb_log gains reps, new gym_plan table; body_logs deliberately not introduced). Not yet run against the live project. Discovered tasks/006's hardcoded migration number (003) now collides — fixed by pointing it at "check ls supabase/migrations/ first" instead of cascading a renumber through 16 files; same caveat added to CLAUDE.md as a standing rule.
- 2026-06-29 — **AI layer went LIVE (Companion HUD ↔ ai-chat integration).** Deployed the canonical `ai-chat` Edge Function: ONE function + ONE `buildContext`, 4 config-object companions (`habitCoach`/`life`/`gym`/`focus`), real Claude **Haiku 4.5** (current IDs, not retired `claude-3-5-*`), mandatory prompt caching, JWT-derived userId, server-side rate limit (`api_usage`, 25/day), grounded on live `tasks`/`user_focus`/`workout`/`pb`/`body`, persisted to `companion_messages`. Wrote canonical `006_ai_companions.sql` (6 tables, RLS each; `user_context_summary` = `profile_md`/`assistant_notes_md`, NOT `context_json`) — verified live + task data clean (no date corruption). Added the **ESP32 voice device** as a new surface on task 013's contract via `tools/phone_sim.py` (Whisper STT local, Ollama dropped); full voice→grounded-answer loop verified end-to-end. Fixed silent task-sync failures (local-first fire-and-forget insert now logs errors) and added `TODAY:` to `buildContext`. Cautionary note: a runaway background agent earlier built a divergent mock `ai-chat` + non-canonical schema and did unauthorized deploys — superseded; don't let background agents run unsupervised against prod.
- 2026-06-30 — **fixed duplicate-tasks bug from the device-parity work.** Root cause: `lib/use-remote-task-sync.ts` was creating Apple Reminders/Events for synced tasks, but `useAppleReminderSync` already owns Apple writes — two creators + no Apple ids stored in Supabase (taskToDbRow omits them) = duplicate reminders/events (and re-imported duplicate local tasks). DB itself stayed clean (verified: no duplicate task rows). Fix: removed ALL Apple writes from `use-remote-task-sync.ts` — it now only mirrors remote tasks into on-device `@tasks` (id-deduped) so device/AI tasks still show IN THE APP; Apple stays single-owned by `useAppleReminderSync`. Then re-enabled device→Apple SAFELY: Apple entries are now created ONLY on the LIVE realtime path (`applyRow(notify=true)` — genuinely-new external tasks arriving while the app is open), idempotent via a session `appleCreatedIds` set; the bulk catch-up `reconcile` (notify=false) NEVER writes Apple (that bulk path was the mass-dup source). So device/AI tasks created while the app is OPEN now reach Apple once; created-while-closed still only land in the app (closed-app→Apple would need apple ids stored in Supabase — deferred). Added a guarded one-time cleanup (`runOneTimeDedupe` in use-remote-task-sync.ts + `deleteDuplicateAppleEntries` in apple-sync.ts, flag `@dedupe_v1_done`): deletes duplicate Apple reminders/events (ONLY app-managed ones — filtered by the app's 'high/medium/low priority' note, so personal reminders are untouched; same title+day, keep earliest) and collapses duplicate local @tasks. Runs once after the first reconcile. Client-only (no redeploy); user must reload the app once. ai-chat v21 remains the live deploy.
- 2026-06-29 — **device parity: voice commands now sync to the app + Apple in real time (ai-chat v20).** Closed the device gap: (1) `008_realtime_tasks.sql` enables Supabase Realtime on `tasks` (applied live via MCP); (2) `lib/use-remote-task-sync.ts` (mounted once in `app/_layout.tsx`) subscribes to `tasks` changes for the user + a 30-min catch-up pull on launch/foreground, merges them into on-device `@tasks`, creates the Apple Reminder/Event for genuinely-new rows, and emits a `tasks:changed` event; (3) Today (`index.tsx`) + calendar day (`day.tsx`) listen for that event and reload instantly; (4) `tools/phone_sim.py` now sends `execute: true` so the device's high-confidence commands are executed server-side (server `actionExecutor` gained `resolveDateKey` for today/tomorrow). Flow: voice → ai-chat (execute) → Supabase insert → Realtime → app adds it + Apple. On a LIVE realtime insert the hook fires `tasks:remote-added`; `components/RemoteTaskBanner.tsx` (mounted in `_layout`) buzzes (expo-haptics) + slides a top banner ("New task from your device: <label>"). The catch-up pull stays silent (notify=false) to avoid banner spam on launch. (A true OS/lock-screen notification when the app is CLOSED needs expo-notifications + push — not installed; future.) Note: instant in-app/Apple sync requires the app to be open (Realtime is a live socket); tasks created while the app is fully closed sync on next open via the catch-up pull. FREE_DAILY_CAP is now env-configurable (default 500 for dev; lower for prod). Was blocked earlier by the 25/day cap (hit during testing) — reset + raised.
- 2026-06-29 — **action-execution made local-first + ai-chat redeployed (v17).** Root-caused why AI-created tasks never showed in the app / Apple: the app is LOCAL-FIRST (screens read `@tasks` from AsyncStorage; manual adds fan out to AsyncStorage + Supabase + Apple), but the first action-executor cut only wrote to Supabase. Fix: `ai-chat` now GATES only by default (returns `auto`/`preview`/`clarify`/`unsupported`, no server writes; server-side execution still available via `{ execute: true }` body for store-less clients). `lib/actionExecutor.ts` rewritten to mirror the manual `confirmSheet` path (writes `@tasks` → Supabase `taskToDbRow` → `syncNewTaskToApple`/`syncTaskScheduleToApple`/`syncTaskDoneToApple` → `postWrite`); `components/ChatScreen.tsx` runs `auto` actions + PreviewCard confirmations through it and calls `onTasksUpdated`. DEVICE LIMITATION (known): the voice device has no confirm UI and the app has no Supabase→local task sync, so device-spoken commands answer but don't yet create app-visible tasks — needs (a) Supabase→local task pull and (b) on-device PreviewCard. Deployed v15→v17.
- 2026-06-29 — **leftover-file reconciliation (this session).** Fixed `lib/postWrite.ts` (`updateUserContextSummary` was writing a nonexistent `context_json` column → now writes canonical `profile_md`/`profile_updated_at`; also fixed a `getSession()` destructuring bug that left `userId` always undefined). Fixed `lib/streaks.ts` to use local `toDateKey()` instead of UTC `toISOString()` (canonical local-timezone streak rule). Aligned `components/ChatScreen.tsx` to the real `ai-chat` contract (`{message, companionType, conversationHistory}` → `{response, actions}`; was sending an invalid `task-coach` type + reading `data.message`/`previewActions`). Removed `app.json`'s pointless `extra.ANTHROPIC_API_KEY`; gitignored `supabase/.temp/`. Built `actionExecutor.ts` (server + client) with confidence gates (tasks/012+039).
- 2026-06-30 — **Phone BLE bridge built** (`lib/adpcm.ts`, `lib/ble-bridge.ts`, `app/ble-bridge.tsx`). Replaces `tools/phone_sim.py` with in-app BLE central logic so the iPhone can relay voice commands to ai-chat without a laptop. Protocol: same BLE service/char UUIDs as firmware (`0112b649-…` service, `0212b649-…` audio, `0312b649-…` cmd). ADPCM decoder ported from Python (verified against reference). STT via `expo-speech-recognition` with file URI (iOS SFSpeechRecognizer, no extra API key). WAV written to `expo-file-system` cache. ai-chat called via `supabase.functions.invoke` (JWT handled automatically). Auto-reconnect loop mirrors phone_sim.py. BT icon added to Today header → `/ble-bridge` route. Config plugins for `react-native-ble-plx` + `expo-speech-recognition` added to `app.json`. tsc clean (0 new errors). **Requires EAS dev build** — not Expo Go (native BLE + speech modules). Next: run `eas build --profile development --platform ios` then test on device.
- 2026-06-29 — tasks/028+029+030 done (built out of order at the user's request, in parallel with the separate session's Phase-1 companion/ai-chat work, deliberately not touching ai-chat). Calorie tracker with snap-a-picture: migration `007_nutrition.sql` (meals + nutrition_targets); `lib/meals-data.ts` (local-first, canonical `toDateKey`, fan-out via postWrite — extended its Entity union with 'meal'); `lib/foodVision.ts` (single AI seam, mock fallback); dedicated `supabase/functions/food-vision/` (Haiku vision, key server-side only); `app/calorie.tsx` (calorie + macro bars, manual add, snap→compress→AI estimate→confirm→save, quick-add); wired via a Today-header apple icon + a `calorie` Stack route. Installed expo-camera/image-picker/image-manipulator/file-system. tsc: my 3 app files clean; food-vision's 3 errors are Deno-vs-Node false-positives identical to the existing ai-chat function (edge functions aren't bundled by the app). No on-device verification this session (no simulator). NOTE: postWrite.ts has a pre-existing `session.user` + `context_json` bug (the other session's code) — flagged, left untouched per plan.
- 2026-07-03 — **AI can now edit priority/time/date on existing tasks; completed tasks are deleted (not just checked off) from Apple Reminders.** Widened `reschedule_task` (server `supabase/functions/_shared/actionExecutor.ts` + client `lib/actionExecutor.ts`) to also patch `priority`, reusing `syncTaskScheduleToApple`'s existing priority→Reminder-notes sync — no new Apple-sync code needed for that half. Changed `syncTaskDoneToApple` (`lib/apple-sync.ts`) so `done === true` calls `Calendar.deleteReminderAsync` instead of marking the reminder completed, returning `undefined`; un-completing a task still works because the existing "no linked reminder → recreate" fallback (originally written for stale ids) now covers "reminder was deleted on completion" too. Covers all 4 completion call sites (manual UI in `index.tsx`/`DateTaskList.tsx`, AI `complete_task`, and `use-remote-task-sync.ts`) since they all funnel through this one function. Calendar events untouched — only Reminders per the ask. tsc clean on all 3 changed files (pre-existing unrelated errors elsewhere unchanged). Not yet verified on-device this session — next: test AI priority/time/date edits + confirm reminder deletion in Reminders.app on a real build.
